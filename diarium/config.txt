[journal]
# Set this to your favorite texteditor.
# Make sure it doesn't fork and detach on execution.
editor = /usr/bin/gvim --nofork
reader = /usr/bin/gvim --nofork

[DEFAULT]

# Can be left empty
fileExtension = .txt

journalPath = ~/journal

# for the different dateFormat and timeFormat directives
# have a look at Python's time.strftime documentation  
dateFormat = %Y-%m-%d
timeFormat = %H:%M:%S